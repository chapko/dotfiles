#!/usr/bin/env bash

set -e

if [[ $# -gt 0 ]]; then
    selected="$1"
else
    selected="$(find ~ ~/.config ~/workspace ~/workspace/rss /etc -mindepth 1 -maxdepth 1 -type d | fzf)"
fi

(
    cd "${selected}" && "$EDITOR"
)
